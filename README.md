### Intro

This is a quick nodeJs server for sending multiple messages to devices using Twilio messaging API.

### Running server

- Install dependacies
  `yarn install`
- Run server dev
  `yarn start`
